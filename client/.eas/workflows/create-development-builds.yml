name: Create development builds

on:
    push:
        branches:
            - dev
jobs:
    android_development_build:
        name: Build Android
        type: build
        params:
            platform: android
            profile: development
